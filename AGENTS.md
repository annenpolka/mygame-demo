# 開発の区切り

- 依頼を実装・動作確認した意味のある単位でコミットする。UI、戦闘ルール、AI・実験、音など、個別にレビュー・復元できる境界を選ぶ。
- コミット前に差分を確認し、変更に対応するテスト、ビルド、`git diff --check` を通す。生成した実験結果は条件・バージョン・再現方法を添える。
- 未完了の変更や無関係なファイルを同じコミットへ混ぜない。進行中に受け取った追加依頼も、適切な区切りまで作業して別コミットにする。
- このリポジトリの GitHub remote は private を維持する。プッシュ前にステージした差分の機密情報を点検し、プッシュ後は remote の実際の参照とローカル HEAD の一致を確認する。
- 大きな作業では、完了した単位ごとにコミットし、依頼全体の検証後にプッシュする。ユーザーから順序指定があればそれに従う。
- コアの計算や処理順を変える場合は `src/content/data.ts` の `VERSION` を更新し、旧版の途中状態をそのまま再操作させない。数値・定義の識別と、入力再生による保存State一致を両方確認する。説明だけの修正とバランス変更は別コミットにする。
